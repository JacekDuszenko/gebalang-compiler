DECLARE
    a, b, c(10:20)
BEGIN
    READ a;
    IF a GEQ 0 THEN
	WHILE a GE 0 DO
	    b ASSIGN b DIV 2;
	    b ASSIGN 2 TIMES b;
	    IF a GE b THEN
                WRITE 1;
	    ELSE
                WRITE 0;
	    ENDIF
	    c(10) ASSIGN a DIV 2;
	ENDWHILE
    ENDIF
END